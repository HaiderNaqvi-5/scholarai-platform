# Alembic configuration
# Run: alembic init alembic
# Then update alembic/env.py to import Base and set target_metadata = Base.metadata

[alembic]
script_location = alembic
sqlalchemy.url = postgresql://scholarai:password@localhost:5432/scholarai
