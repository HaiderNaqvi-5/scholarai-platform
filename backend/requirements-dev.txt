-r requirements.txt

pytest==8.3.0
pytest-asyncio==0.24.0
# Test-only: Playwright Python for tests/e2e/playwright browser-smoke jobs.
# Chromium binary NOT in prod image; CI fetches it in a separate step.
playwright==1.52.0
