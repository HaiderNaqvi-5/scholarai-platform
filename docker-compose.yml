services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file: ${ENV_FILE:-.env.example}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      opensearch:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/livez || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3
    networks:
      - internal
      - public

  frontend:
    build:
      context: ./frontend
    container_name: frontend
    ports:
      - "3000:3000"
    env_file: ${ENV_FILE:-.env.example}
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - public

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: scholarai
      POSTGRES_USER: scholarai
      POSTGRES_PASSWORD: ${DB_PASSWORD:-password}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scholarai -d scholarai"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - internal
      - public
    volumes:
      - pgdata:/var/lib/postgresql/data

  neo4j:
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-password}
    ports:
      - "7474:7474"
      - "7687:7687"
    healthcheck:
      # TCP probe on Bolt port. Avoids cypher-shell JVM cold-start (>5s) which
      # causes false-unhealthy under load. Bolt listening = server ready for queries.
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/7687' || exit 1"]
      interval: 15s
      timeout: 5s
      start_period: 30s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 2G
    networks:
      - internal
    volumes:
      - neo4jdata:/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - internal

  opensearch:
    image: opensearchproject/opensearch:2.11.0
    environment:
      - cluster.name=scholarai-cluster
      - node.name=opensearch-node1
      - discovery.type=single-node
      - bootstrap.memory_lock=true
      - "OPENSEARCH_JAVA_OPTS=-Xms1024m -Xmx1024m"
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=${OPENSEARCH_PASSWORD:-ScholarAI_Secure_123!}
      - DISABLE_SECURITY_PLUGIN=true
      - DISABLE_INSTALL_DEMO_CONFIG=true
    deploy:
      resources:
        limits:
          memory: 2G
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - "9200:9200"
      - "9600:9600"
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\"\\|\"status\":\"yellow\"'"]
      interval: 20s
      timeout: 10s
      retries: 5
    networks:
      - internal
    volumes:
      - osdata:/usr/share/opensearch/data

  celery-worker:
    build:
      context: ./backend
    command: celery -A app.tasks.celery_app:celery_app worker -l info
    env_file: ${ENV_FILE:-.env.example}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      opensearch:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "celery -A app.tasks.celery_app:celery_app inspect ping -d celery@$$HOSTNAME -t 8 || exit 1"]
      interval: 60s
      timeout: 15s
      start_period: 45s
      retries: 3
    networks:
      - internal

  celery-beat:
    build:
      context: ./backend
    command: celery -A app.tasks.celery_app:celery_app beat -l info
    env_file: ${ENV_FILE:-.env.example}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      opensearch:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "grep -q beat /proc/1/cmdline || exit 1"]
      interval: 60s
      timeout: 10s
      start_period: 30s
      retries: 3
    networks:
      - internal

networks:
  internal:
    internal: true
  public:

volumes:
  pgdata:
  neo4jdata:
  osdata:
