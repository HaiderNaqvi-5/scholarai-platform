import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";
export const metadata = { title: "Record" };
export default function CurationRecordPage() {
  return (
    <PlaceholderRoute
      title="Curation record"
      blurb="Edit fields, approve, reject, publish, unpublish. Full audit log."
      sprint="Sprint 8"
    />
  );
}
