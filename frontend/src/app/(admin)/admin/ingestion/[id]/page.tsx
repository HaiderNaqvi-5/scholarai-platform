import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";
export const metadata = { title: "Run detail" };
export default function IngestionDetailPage() {
  return (
    <PlaceholderRoute
      title="Ingestion run"
      blurb="Diagnostics, capture path, failure phase, raw HTML viewer."
      sprint="Sprint 8"
    />
  );
}
