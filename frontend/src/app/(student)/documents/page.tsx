import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";

export const metadata = { title: "Documents" };

export default function DocumentsPage() {
  return (
    <PlaceholderRoute
      title="Your documents"
      blurb="Statements of purpose, motivation letters, essays. Each gets grounded feedback split into validated facts, retrieved guidance, generated guidance, and limitations."
      sprint="Sprint 5"
    />
  );
}
