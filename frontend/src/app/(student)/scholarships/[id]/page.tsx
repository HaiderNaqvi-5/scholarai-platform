import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";

export const metadata = { title: "Scholarship" };

export default function ScholarshipDetailPage() {
  return (
    <PlaceholderRoute
      title="Scholarship detail"
      blurb="Title, deadline, amount, eligibility chips, and Save above the fold. Description and requirements below."
      sprint="Sprint 3"
    />
  );
}
