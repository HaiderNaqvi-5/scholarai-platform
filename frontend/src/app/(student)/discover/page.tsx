import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";

export const metadata = { title: "Discover" };

export default function DiscoverPage() {
  return (
    <PlaceholderRoute
      title="Discover"
      blurb="Filter all published scholarships by country, field, deadline, funding type, and amount. Filter state lives in the URL — share or bookmark any view."
      sprint="Sprint 3"
    />
  );
}
