import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";
export const metadata = { title: "Settings" };
export default function SettingsPage() {
  return (
    <PlaceholderRoute
      title="Settings"
      blurb="Account, sessions, sign out. Email and password changes."
      sprint="Sprint 2"
    />
  );
}
