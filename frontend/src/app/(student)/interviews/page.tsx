import { PlaceholderRoute } from "@/components/shell/PlaceholderRoute";
export const metadata = { title: "Interviews" };
export default function InterviewsPage() {
  return (
    <PlaceholderRoute
      title="Interview practice"
      blurb="Adaptive question loop scoped to general or scholarship-specific prep. Rubric scores per dimension, follow-ups target your weakest area."
      sprint="Sprint 6"
    />
  );
}
