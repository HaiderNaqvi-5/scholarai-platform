# Frontend will be initialized with Next.js 14
# Run: npx -y create-next-app@latest ./ --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
# This README serves as a placeholder until the Next.js app is scaffolded

## Setup
```bash
cd frontend
npx -y create-next-app@latest ./ --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
npm run dev
```

## Planned Pages
- `/` — Landing page
- `/dashboard` — Student dashboard with match results
- `/scholarships` — Browse and search scholarships
- `/scholarships/[id]` — Scholarship detail + SHAP explanation
- `/interview` — Interview simulator
- `/profile` — Student profile management
- `/mentors` — Browse mentors
- `/admin` — Admin dashboard
