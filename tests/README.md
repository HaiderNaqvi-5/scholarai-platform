# ScholarAI Tests

This top-level test tree mirrors the new monorepo structure.

Subdirectories:

- `api/` for API and domain tests
- `web/` for UI smoke and component tests
- `workers/` for task and queue tests
- `integration/` for end-to-end or cross-surface checks
