# Shared

This directory holds shared contracts, constants, and future cross-app references that should not be owned exclusively by `frontend/` or `backend/`.

Current MVP stance:
- Keep shared artifacts minimal.
- Prefer documentation and type contracts over reusable runtime abstractions in the first coding phase.
