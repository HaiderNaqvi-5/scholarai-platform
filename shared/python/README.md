# Shared Python

This folder is reserved for future shared Python contracts and neutral utilities used by the API and worker surfaces.

It stays intentionally empty at scaffold time to avoid premature coupling.
